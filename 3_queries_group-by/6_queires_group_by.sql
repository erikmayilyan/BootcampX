SELECT students.name as student, AVG(assignments_submissions.duration) AS average_assignment_duration, AVG(assignments.duration) AS average_estimated_duration FROM student JOIN assignment_submissions ON students.id = student_id JOIN assignment_submissions ON assignment_submissions.id = assignment_id WHERE student.end_date IS NULL GROUP BY student.name ORDER BY AVG(assignments.duration) 
